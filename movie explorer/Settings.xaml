<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="movie_explorer.Settings"
             Title="Settings">
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="14">
            <Label Text="User" FontAttributes="Bold" FontSize="18" />

            <HorizontalStackLayout Spacing="12">
                <Label Text="Name" VerticalOptions="Center" />
                <Entry Text="{Binding UserName}" HorizontalOptions="FillAndExpand" Placeholder="Your name" />
            </HorizontalStackLayout>

            <BoxView HeightRequest="1" Opacity="0.15" />

            <Label Text="Appearance" FontAttributes="Bold" FontSize="18" />

            <HorizontalStackLayout Spacing="12">
                <Label Text="Theme" VerticalOptions="Center" />
                <Picker ItemsSource="{Binding ThemeOptions}"
                        SelectedItem="{Binding SelectedTheme}"
                        HorizontalOptions="FillAndExpand" />
            </HorizontalStackLayout>

            <Button Text="Apply" Command="{Binding ApplyThemeCommand}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>